import {
  Modal,
  ModalOverlay,
  ModalContent,
  ModalHeader,
  ModalFooter,
  ModalBody,
  ModalCloseButton,
  IconButton,
  Text,
  Stack,
  Heading,
  Button,
  Box,
} from '@chakra-ui/react';
import { useDisclosure } from '@chakra-ui/react';
import { FaCog } from 'react-icons/fa';
import React from 'react';

function AboutModal() {
  const { isOpen, onOpen, onClose } = useDisclosure();
  return (
    <>
      <Text
        textAlign={'left'}
        userSelect={'none'}
        opacity={0.6}
        onClick={onOpen}
        _hover={{
          opacity: 1,
          cursor: 'pointer',
        }}
      >
        Image Generated by AI
      </Text>
      <Modal isOpen={isOpen} onClose={onClose}>
        <ModalOverlay />
        <ModalContent>
          <ModalHeader>About</ModalHeader>
          <ModalCloseButton />
          <ModalBody>
            <Stack gap={4}>
              <Stack>
                <Heading size={'sm'}>Did you know?</Heading>
                <Text>
                  Both the image and the quote you're seeing were generated by
                  AI
                </Text>
              </Stack>
              <Stack>
                <Heading size={'sm'}>Image Prompt</Heading>
                <Box bg={'blackAlpha.200'} p={4} rounded={'md'}>
                  <Text>Lorem ipsum, etc, etc.</Text>
                </Box>
              </Stack>
            </Stack>
          </ModalBody>
          <ModalFooter></ModalFooter>
        </ModalContent>
      </Modal>
    </>
  );
}

export default AboutModal;
